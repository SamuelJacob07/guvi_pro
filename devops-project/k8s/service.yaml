apiVersion: v1
kind: Service
metadata:
  name: devops-demo-app
  namespace: devops-demo
spec:
  type: LoadBalancer
  selector:
    app: devops-demo-app
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
